<%- include('header.ejs') %>

<div class="products">
  <div class="container">
    <div class="cargos product-type">
      <h1 class="heading">CARGOS</h1>
      <%if(locals.cargos){%>
      <% for(let i=0;i<cargos.length;i++){%>
      <div class="item">
        <img src="/gallery/<%=cargos[i].image%>" alt="Image of shirt">
        <div class="item-details">
          <p class="name"><%=cargos[i].name%></p>
          <div class="item-price row">
            <p class="price col-3">₹<%=cargos[i].price%></p>
            <p class="original-price col-3">₹<%=cargos[i].price+600%></p>
          </div>
        </div>
        <div class="hidden_forms">
          <form action="/addToWishlist" method="post">
            <!-- Add your product ID input field inside the form -->
            <input type="hidden" name="productId" value="<%=cargos[i].id%>">
            <!-- Add other form fields if needed -->

            <button type="submit" class="wishlist btn btn-primary btn-sm">Add to wishlist</button>
          </form>

          <form action="/addToCart" method="post">
            <!-- Add your product ID input field inside the form -->
            <input type="hidden" name="productId" value="<%=cargos[i].id%>">
            <!-- Add other form fields if needed -->

            <button type="submit" class="wishlist btn btn-success btn-sm">Add to cart</button>
          </form>
        </div>
      </div>
      <%}}%>
    </div>
  </div>
</div>

<%- include('footer.ejs') %>